module EnrollmentsHelper
  # def enroll_course course_code
  #
  #   if !Course.where(course_code: course_code).nil?
  #     new_enrollment = Enrollment.new(course_id: Course.where(course_code: course_code).id, user_id: User.find(session[:user_id]).id)
  #     new_enrollment.save
  #   end
  # end

end
