<%= form_with(model: assignment, local: true) do |form| %>
  <% if assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :due_date %>
    <%= form.datetime_select :due_date, id: :assignment_due_date %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.number_field :course_id, id: :assignment_course_id %>
  </div>

  <div class="field">
    <%= form.label :instructions %>
    <%= form.text_area :instructions, id: :assignment_instructions %>
  </div>

  <div class="field">
    <%= form.label :template %>
    <%= form.text_field :template, id: :assignment_template %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
