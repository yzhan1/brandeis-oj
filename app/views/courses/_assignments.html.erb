<div class="col">
  <h4 style="display: flex; margin-bottom: 10px" class="justify-content-between">
    <div>Posted Assignments</div>
    <% if !is_student? %>
      <%= link_to 'New Assignment', new_assignment_path(:course_id => @course.id), class: "btn btn-outline-primary btn-sm btn-link" %>
    <% end %>
  </h4>
  <table class="table table-striped table-bordered">
    <thead>
    <tr>
      <th>Assignment Name</th>
      <th>Due Date</th>
      <% if !is_student? %>
        <th>Actions</th>
        <th>Submissions</th>
      <% end %>
    </tr>
    </thead>
    <tbody>
    <% if @assignment_list %>
      <% @assignment_list.each do |assignment| %>
        <tr>
          <td><%= link_to assignment.name, assignment %></td>
          <% if assignment.pass_due? && is_student? %>
          <td style="color: red">
          <% else %>
          <td>
          <% end %>
            <%= assignment.due_date.strftime('%b %d %Y, %H:%M') %>
          </td>
          <% if !is_student? %>
            <td>
              <div class="btn-group" role="group">
                <%= link_to 'Edit', edit_assignment_path(assignment, course_id: @course.id), class: "btn btn-outline-primary btn-sm" %>
                <%= link_to 'Delete', assignment, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %>
              </div>
            </td>
            <td>
              <div class="btn-group" role="group">
                <%= link_to 'Show', assignment, class: "btn btn-outline-success btn-sm" %>
                <%= link_to 'Run Tests', test_path(assignment_id: assignment.id.to_s), class: "btn btn-outline-info btn-sm", remote: true %>
              </div>
            </td>
          <% end %>
        </tr>
      <% end %>
    <% else %>
      <td colspan="4" align="center">No assignments</td>
    <% end %>
    </tbody>
  </table>
  <% if is_student? %>
    <%= render 'submissions' %>
  <% end %>
</div>
