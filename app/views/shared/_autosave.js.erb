// setInterval(autosave, 5000)
const autosave = () => {
  $.ajax({
    type: 'PATCH',
    url: '/autosave',
    data: $('form').serialize(),
    dataType: 'script',
    success: (data) => {
      console.log(data)
    }
  })
}